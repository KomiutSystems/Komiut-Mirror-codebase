/.phpunit.cache
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/framework/sessions/*
/vendor
enviroments/.env
.env.backup
.env.production
.phpunit.result.cache
Homestead.json
Homestead.yaml
auth.json
npm-debug.log
yarn-error.log
/.fleet
/.idea
/.vscode
sessions/*
sessions/*

# --- Secrets: never commit real env files or service-account credentials ---
Docker/*/.env.*
enviroments/.env.save
storage/app/json/*firebase-adminsdk*.json
*.pem
*.p12
.env

# Agent worktrees. `git add -A` otherwise commits these as embedded git
# repositories, which clones cannot fetch and which nothing here wants tracked.
.claude/worktrees/
