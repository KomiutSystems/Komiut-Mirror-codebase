name: "Driver — the app's home, earnings and expenses"
description: |-

  Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
  never on the driver's own history. Drivers rotate between matatus, so the
  plate is what identifies a day's takings: money is paid to the vehicle's till,
  not to the person. A driver who moved buses this morning must see this bus.

  Access is by IDENTITY, not permission. These read only the caller's own
  assigned vehicle, so a permission adds nothing that the assignment does not
  already enforce — and gating them would 403 the 206 migrated crew, who hold
  `Conductor` and therefore lack `Edit Queues`.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/driver/home
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: 'The home screen'
      description: |-
        Today's running total, the vehicle's capacity, and the most recent
        payments — one call, because three round trips from Nairobi to Frankfurt
        costs about three seconds of a driver staring at spinners.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/driver/earnings
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: "Today's takings"
      description: |-
        The running total, from the day's first payment to its most recent. A
        driver checks this through the day and again when they knock off; the
        `first_at`/`last_at` pair is what makes "so far" meaningful rather than
        just a number.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/driver/transactions
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Recent payments, newest first'
      description: |-
        Paginated at 20. The dashboard's `transactions` endpoint is SACCO-wide —
        a driver reading it sees every other bus in the SACCO — so this one is
        confined to the assigned vehicle.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/driver/bookings
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: 'The bookings screen'
      description: |-
        Passengers on the current queue, filterable by status. Unlike
        trips/bookings this does not require `Edit Queues`, so a conductor can
        read the manifest they are actually working.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/driver/expenses
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: 'What the driver spent today'
      description: |-
        Fuel, parking, the stage fee. Takings alone do not tell a driver what they
        are going home with, which is the number they actually care about.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: api/v1/auth/driver/expenses
    metadata:
      custom: []
      groupName: "Driver — the app's home, earnings and expenses"
      groupDescription: |-

        Everything here is keyed on the vehicle the caller is CURRENTLY assigned to,
        never on the driver's own history. Drivers rotate between matatus, so the
        plate is what identifies a day's takings: money is paid to the vehicle's till,
        not to the person. A driver who moved buses this morning must see this bus.

        Access is by IDENTITY, not permission. These read only the caller's own
        assigned vehicle, so a permission adds nothing that the assignment does not
        already enforce — and gating them would 403 the 206 migrated crew, who hold
        `Conductor` and therefore lack `Edit Queues`.
      subgroup: ''
      subgroupDescription: ''
      title: 'Record an expense'
      description: |-
        The driver's own entry, against the vehicle they are assigned to. Nothing
        here takes a vehicle from the request — a driver can only ever spend
        against the bus they are on.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      expense_fee_id:
        custom: []
        name: expense_fee_id
        description: 'Must match an existing stored value.'
        required: true
        example: 16
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      amount:
        custom: []
        name: amount
        description: 'Must be at least 1.'
        required: true
        example: 22
        type: number
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      trans_date:
        custom: []
        name: trans_date
        description: 'Must be a valid date.'
        required: false
        example: '2026-08-15T12:18:16'
        type: string
        enumValues: []
        exampleWasSpecified: false
        nullable: true
        deprecated: false
    cleanBodyParameters:
      expense_fee_id: 16
      amount: 22
      trans_date: '2026-08-15T12:18:16'
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
