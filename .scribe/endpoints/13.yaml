name: 'Roles & permissions'
description: |-

  The dashboard renders per-permission — the login response already returns the
  signed-in user's `permissions`, and the UI shows/hides modules off that.

  Roles are global (no per-SACCO custom roles yet), so **role CRUD is
  superadmin-only**: a created role would otherwise be visible to every SACCO.
  SACCO admins get an **assign-only** surface — they may place their own staff
  on the shared SACCO-tier roles, and only on roles whose permissions they
  themselves hold.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/roles
    metadata:
      custom: []
      groupName: 'Roles & permissions'
      groupDescription: |-

        The dashboard renders per-permission — the login response already returns the
        signed-in user's `permissions`, and the UI shows/hides modules off that.

        Roles are global (no per-SACCO custom roles yet), so **role CRUD is
        superadmin-only**: a created role would otherwise be visible to every SACCO.
        SACCO admins get an **assign-only** surface — they may place their own staff
        on the shared SACCO-tier roles, and only on roles whose permissions they
        themselves hold.
      subgroup: ''
      subgroupDescription: ''
      title: 'List assignable roles'
      description: |-
        Superadmins see every role; SACCO admins see only the shared SACCO-tier
        roles they may assign to staff. Each role carries its permission list.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/permissions
    metadata:
      custom: []
      groupName: 'Roles & permissions'
      groupDescription: |-

        The dashboard renders per-permission — the login response already returns the
        signed-in user's `permissions`, and the UI shows/hides modules off that.

        Roles are global (no per-SACCO custom roles yet), so **role CRUD is
        superadmin-only**: a created role would otherwise be visible to every SACCO.
        SACCO admins get an **assign-only** surface — they may place their own staff
        on the shared SACCO-tier roles, and only on roles whose permissions they
        themselves hold.
      subgroup: ''
      subgroupDescription: ''
      title: 'List the permission catalog'
      description: |-
        Grouped by module (the noun after the View/Add/Edit verb) for building a
        role editor.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: api/v1/auth/roles/save
    metadata:
      custom: []
      groupName: 'Roles & permissions'
      groupDescription: |-

        The dashboard renders per-permission — the login response already returns the
        signed-in user's `permissions`, and the UI shows/hides modules off that.

        Roles are global (no per-SACCO custom roles yet), so **role CRUD is
        superadmin-only**: a created role would otherwise be visible to every SACCO.
        SACCO admins get an **assign-only** surface — they may place their own staff
        on the shared SACCO-tier roles, and only on roles whose permissions they
        themselves hold.
      subgroup: ''
      subgroupDescription: ''
      title: 'Create or update a role (superadmin)'
      description: |-
        Superadmin-only: a created role is global (visible to every SACCO) until
        per-SACCO custom roles land.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      name:
        custom: []
        name: name
        description: 'Role name.'
        required: true
        example: Cashier
        type: string
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
      permissions:
        custom: []
        name: permissions
        description: 'Permission names to grant.'
        required: true
        example:
          - 'View Dashboard'
          - 'View Transactions'
        type: 'string[]'
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanBodyParameters:
      name: Cashier
      permissions:
        - 'View Dashboard'
        - 'View Transactions'
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: 'api/v1/auth/saccos/members/{user_id}/roles'
    metadata:
      custom: []
      groupName: 'Roles & permissions'
      groupDescription: |-

        The dashboard renders per-permission — the login response already returns the
        signed-in user's `permissions`, and the UI shows/hides modules off that.

        Roles are global (no per-SACCO custom roles yet), so **role CRUD is
        superadmin-only**: a created role would otherwise be visible to every SACCO.
        SACCO admins get an **assign-only** surface — they may place their own staff
        on the shared SACCO-tier roles, and only on roles whose permissions they
        themselves hold.
      subgroup: ''
      subgroupDescription: ''
      title: 'Assign roles to a SACCO member'
      description: |-
        A SACCO admin may assign roles only to a member of their OWN SACCO, only
        from the shared SACCO-tier roles, and only roles whose permissions they
        themselves hold (no privilege escalation). Superadmins are unrestricted.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters:
      user_id:
        custom: []
        name: user_id
        description: 'The ID of the user.'
        required: true
        example: 16
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      user:
        custom: []
        name: user
        description: "The member's user id."
        required: true
        example: 5
        type: integer
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanUrlParameters:
      user_id: 16
      user: 5
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      roles:
        custom: []
        name: roles
        description: 'Role names to set on the member.'
        required: true
        example:
          - 'Fleet Manager'
        type: 'string[]'
        enumValues: []
        exampleWasSpecified: true
        nullable: false
        deprecated: false
    cleanBodyParameters:
      roles:
        - 'Fleet Manager'
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
