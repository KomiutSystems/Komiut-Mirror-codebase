name: 'SACCO — messages to the crew'
description: |-

  A SACCO had no way to reach its own drivers and conductors. The notification
  machinery has always fanned one dispatch out to in-app, realtime and push,
  but nothing on the SACCO side could trigger it — every domain notification
  came from an event (a paid booking), never from a person. So "no service on
  Mashujaa Day" or "the fuel levy changes Monday" travelled by WhatsApp group,
  to whoever happened to be in it.

  The send is a queued fan-out, not an inline loop: a large SACCO is two
  hundred drivers, each costing three channel jobs, and holding the admin's
  request open for that would time out precisely the SACCOs that need it most.
endpoints:
  -
    custom: []
    httpMethods:
      - GET
    uri: api/v1/auth/sacco/announcements
    metadata:
      custom: []
      groupName: 'SACCO — messages to the crew'
      groupDescription: |-

        A SACCO had no way to reach its own drivers and conductors. The notification
        machinery has always fanned one dispatch out to in-app, realtime and push,
        but nothing on the SACCO side could trigger it — every domain notification
        came from an event (a paid booking), never from a person. So "no service on
        Mashujaa Day" or "the fuel levy changes Monday" travelled by WhatsApp group,
        to whoever happened to be in it.

        The send is a queued fan-out, not an inline loop: a large SACCO is two
        hundred drivers, each costing three channel jobs, and holding the admin's
        request open for that would time out precisely the SACCOs that need it most.
      subgroup: ''
      subgroupDescription: ''
      title: 'What this SACCO has already sent'
      description: |-
        Exists so the same notice does not go out three times because nobody could
        tell whether it landed.
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters: []
    cleanBodyParameters: []
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
  -
    custom: []
    httpMethods:
      - POST
    uri: api/v1/auth/sacco/announcements
    metadata:
      custom: []
      groupName: 'SACCO — messages to the crew'
      groupDescription: |-

        A SACCO had no way to reach its own drivers and conductors. The notification
        machinery has always fanned one dispatch out to in-app, realtime and push,
        but nothing on the SACCO side could trigger it — every domain notification
        came from an event (a paid booking), never from a person. So "no service on
        Mashujaa Day" or "the fuel levy changes Monday" travelled by WhatsApp group,
        to whoever happened to be in it.

        The send is a queued fan-out, not an inline loop: a large SACCO is two
        hundred drivers, each costing three channel jobs, and holding the admin's
        request open for that would time out precisely the SACCOs that need it most.
      subgroup: ''
      subgroupDescription: ''
      title: 'Send an announcement to the crew'
      description: |-
        Targets the whole SACCO by default. `vehicle_id` narrows it to one bus's
        crew — the difference between "we are closed tomorrow" and "KDA 123X, go
        to the garage".
      authenticated: true
      deprecated: false
    headers:
      Authorization: 'Bearer {YOUR_SANCTUM_TOKEN}'
      Content-Type: application/json
      Accept: application/json
    urlParameters: []
    cleanUrlParameters: []
    queryParameters: []
    cleanQueryParameters: []
    bodyParameters:
      title:
        custom: []
        name: title
        description: 'Must not be greater than 120 characters.'
        required: true
        example: b
        type: string
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      body:
        custom: []
        name: body
        description: 'Must not be greater than 2000 characters.'
        required: true
        example: 'n'
        type: string
        enumValues: []
        exampleWasSpecified: false
        nullable: false
        deprecated: false
      vehicle_id:
        custom: []
        name: vehicle_id
        description: ''
        required: false
        example: 16
        type: integer
        enumValues: []
        exampleWasSpecified: false
        nullable: true
        deprecated: false
    cleanBodyParameters:
      title: b
      body: 'n'
      vehicle_id: 16
    fileParameters: []
    responses:
      -
        custom: []
        status: 401
        content: '{"message":"Unauthenticated."}'
        headers:
          cache-control: 'no-cache, private'
          content-type: application/json
          access-control-allow-origin: '*'
        description: null
    responseFields: []
    auth:
      - headers
      - Authorization
      - 'Bearer 6g43cv8PD1aE5beadkZfhV6'
    controller: null
    method: null
    route: null
